require 'spec_helper'
require 'models/predictor.rb'
describe Predictor do
  it "should update its settings before save" do
    pending 'find a nice way to test this without the need of a test db'
  end
  context "predict" do
    it "should correctly predict a job" do
      pending 'how can I make a reliable test for this? using a good enough dummy predictor with model and all?'
    end
    it "should give a probability for the predicted label" do
      pending 'same issue as the previous test'
    end
  end
end